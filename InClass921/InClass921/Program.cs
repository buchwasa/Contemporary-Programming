using System;

namespace InClass921
{
    class Program
    {
        static void Main(string[] args)
        {
            // Both are reference types
            int[] alpha = new int[42]; // Declare and instantiate
            Console.WriteLine(alpha); // Passing the reference of the array

            double[] beta = new double[5];
        }
    }
}
